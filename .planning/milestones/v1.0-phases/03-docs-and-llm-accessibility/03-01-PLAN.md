---
phase: 03-docs-and-llm-accessibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/user/getting-started.md
  - docs/user/configuration.md
  - docs/user/providers.md
  - docs/user/output-documents.md
autonomous: true

must_haves:
  truths:
    - 'A user who installs handover-cli can find a getting-started guide in docs/user/ that takes them from install to first output'
    - 'A user can look up any config key in docs/user/configuration.md and find its purpose, type, default value, and valid options'
    - 'A user can compare all 8 LLM providers in a single table in docs/user/providers.md'
    - 'A user can understand what handover outputs (the 14 documents) before running the tool'
  artifacts:
    - path: 'docs/user/getting-started.md'
      provides: 'Install-to-first-output quickstart'
      contains: 'npx handover-cli'
    - path: 'docs/user/configuration.md'
      provides: 'All 21 config keys documented'
      contains: 'provider'
    - path: 'docs/user/providers.md'
      provides: 'Provider system overview and 8-provider comparison table'
      contains: 'anthropic'
    - path: 'docs/user/output-documents.md'
      provides: 'Description of all 14 output documents'
      contains: 'render-'
  key_links:
    - from: 'docs/user/configuration.md'
      to: 'src/config/schema.ts'
      via: 'config key names match schema keys'
      pattern: 'provider|model|apiKeyEnv|baseUrl|timeout|output|audience'
    - from: 'docs/user/providers.md'
      to: 'src/providers/presets.ts'
      via: 'provider names and env vars match presets'
      pattern: 'anthropic|openai|ollama|groq|together|deepseek|azure-openai|custom'
---

<objective>
Create the docs/user/ folder with four user-facing documentation files: a getting-started quickstart, a configuration reference, a provider comparison guide, and an output documents overview.

Purpose: Users who install handover-cli can find authoritative how-to guides without reading source code — satisfying Phase 3 success criterion #1.
Output: Four markdown files in docs/user/
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-docs-and-llm-accessibility/03-CONTEXT.md
@.planning/phases/03-docs-and-llm-accessibility/03-RESEARCH.md
@src/config/schema.ts
@src/config/loader.ts
@src/providers/presets.ts
@src/providers/base-provider.ts
@src/renderers/
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create getting-started.md and configuration.md</name>
  <files>docs/user/getting-started.md, docs/user/configuration.md</files>
  <action>
Create `docs/user/` directory.

**getting-started.md:** Quick-start reference style (locked decision: assumes CLI/Node familiarity). Structure:

1. H1 "Getting started"
2. Prerequisites: Node >= 18, an LLM API key
3. Install: `npm install -g handover-cli` and `npx handover-cli generate` one-liner
4. First run: show minimal `.handover.yml` config (provider + output), then the command, then an inline example of what output looks like (locked decision: inline example output snippets)
5. Common options: brief table of the 3-4 most-used CLI flags (--provider, --output, --static-only)
6. Next steps: link to configuration.md and providers.md

**configuration.md:** Walk through config keys organized by section (locked decision: organized by config file sections). Source ALL keys from `src/config/schema.ts` — the authoritative source. Structure:

1. H1 "Configuration reference"
2. Brief intro: config via `.handover.yml`, CLI flags, or env vars. Note precedence order from `src/config/loader.ts`: CLI flags > Environment variables (HANDOVER_PROVIDER, HANDOVER_MODEL, HANDOVER_OUTPUT) > .handover.yml > Zod defaults
3. H2 "Top-level options" — document each: provider, model, apiKeyEnv, baseUrl, timeout, output, audience, include, exclude, context, costWarningThreshold
4. H2 "Project options" (project.\*) — name, description, domain, teamSize, deployTarget
5. H2 "Analysis options" (analysis.\*) — concurrency, staticOnly
6. H2 "Context window options" (contextWindow.\*) — maxTokens, pin, boost
7. Each key entry: key name in code font, type, default value, description of what it does, valid values where applicable
8. Include a full example `.handover.yml` showing all sections together

Reference real file paths (e.g., `src/config/schema.ts`) but not line numbers — per locked decision.
</action>
<verify>

- Both files exist: `ls docs/user/getting-started.md docs/user/configuration.md`
- getting-started.md contains npx command and example output snippet
- configuration.md documents all 21 config keys (count occurrences of backtick-wrapped key names)
- Config precedence order is stated
  </verify>
  <done>getting-started.md takes a user from zero to first output with inline examples; configuration.md documents all 21 config keys with types, defaults, and descriptions organized by section group</done>
  </task>

<task type="auto">
  <name>Task 2: Create providers.md and output-documents.md</name>
  <files>docs/user/providers.md, docs/user/output-documents.md</files>
  <action>
**providers.md:** Overview + comparison table format (locked decision). Source data from `src/providers/presets.ts` — the authoritative registry. Structure:
1. H1 "Providers"
2. Brief overview of the provider system: handover supports 8 LLM providers through a unified interface. BaseProvider handles retry and rate-limiting; concrete providers implement the completion call.
3. H2 "Provider comparison" — table with columns (per research recommendation): Provider, Env var, Default model, Local?, Notes. All 8 providers: anthropic, openai, ollama, groq, together, deepseek, azure-openai, custom. No per-provider deep-dive sections (locked decision).
4. H2 "Configuring a provider" — short section showing how to set provider in .handover.yml, via env var (HANDOVER_PROVIDER), or CLI flag. Show 2-3 example configs (Anthropic cloud, Ollama local, custom endpoint).
5. H2 "Custom provider" — explain the escape hatch: custom provider uses LLM_API_KEY by default, requires baseUrl, works with any OpenAI-compatible API. Note that apiKeyEnv can override the default env var name.

**output-documents.md:** Describe what handover generates. Structure:

1. H1 "Output documents"
2. Brief intro: handover generates 14 interconnected markdown documents in the output directory
3. Table or numbered list of all 14 documents with: document number (00-13), document name, brief description of what it covers. Source from `src/renderers/` — each renderer file name maps to a document.
4. H2 "Document relationships" — brief note that documents cross-reference each other and are designed to be read together or individually
5. Include an inline example snippet showing what a rendered document looks like (locked decision: inline example output snippets)
   </action>
   <verify>

- Both files exist: `ls docs/user/providers.md docs/user/output-documents.md`
- providers.md table has exactly 8 rows (one per provider)
- providers.md env var names match src/providers/presets.ts
- output-documents.md lists all 14 documents
  </verify>
  <done>providers.md has an 8-provider comparison table with accurate env vars and default models sourced from presets.ts; output-documents.md describes all 14 rendered documents with example output</done>
  </task>

</tasks>

<verification>
1. `ls docs/user/` shows 4 files: getting-started.md, configuration.md, providers.md, output-documents.md
2. configuration.md references all 21 config keys from src/config/schema.ts
3. providers.md table has 8 rows matching the 8 providers in src/providers/presets.ts
4. output-documents.md lists 14 documents matching src/renderers/ count
5. All docs use quick-start reference style (not tutorial hand-holding) per locked decision
6. All docs include inline example output snippets per locked decision
7. File paths referenced in docs are real (e.g., `src/config/schema.ts` exists) — no line numbers per locked decision
</verification>

<success_criteria>

- A user who has never seen handover can go from install to first output using only docs/user/getting-started.md
- A user can look up any config key and find its purpose, type, default, and valid values in configuration.md
- A user can compare all 8 providers at a glance in providers.md
- A user can understand what 14 documents handover produces before running it using output-documents.md
  </success_criteria>

<output>
After completion, create `.planning/phases/03-docs-and-llm-accessibility/03-01-SUMMARY.md`
</output>
