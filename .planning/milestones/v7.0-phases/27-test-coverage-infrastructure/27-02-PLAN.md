---
phase: 27-test-coverage-infrastructure
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - src/renderers/utils.test.ts
  - src/config/schema.test.ts
  - src/context/packer.test.ts
autonomous: true

must_haves:
  truths:
    - "renderers/utils.test.ts covers buildSummaryLine with tests for all 4 switch cases (architecture, overview, dependencies, testing) plus the default fallback, each asserting on returned string content"
    - "config/schema.test.ts covers the EmbeddingConfigSchema superRefine validation — rejects local-only mode without local.model, accepts remote-only without local.model"
    - "context/packer.test.ts covers the edge-case marker detection path (lines 154-177) and the greedy section subset selection path (lines 366-398)"
    - "All new tests assert on actual return values/output, not just mock call counts (per locked decision: unit-focused with real outputs)"
  artifacts:
    - path: "src/renderers/utils.test.ts"
      provides: "buildSummaryLine tests for all switch cases and default"
      contains: "buildSummaryLine"
    - path: "src/config/schema.test.ts"
      provides: "EmbeddingConfigSchema superRefine validation tests"
      contains: "EmbeddingConfigSchema"
    - path: "src/context/packer.test.ts"
      provides: "Edge-case marker and greedy section subset tests"
      contains: "marker"
  key_links:
    - from: "src/renderers/utils.test.ts"
      to: "src/renderers/utils.ts"
      via: "import buildSummaryLine"
      pattern: "buildSummaryLine"
    - from: "src/config/schema.test.ts"
      to: "src/config/schema.ts"
      via: "import HandoverConfigSchema or EmbeddingConfigSchema"
      pattern: "EmbeddingConfigSchema|embedding"
---

<objective>
Expand existing test suites for `renderers/utils`, `config/schema`, and `context/packer` to cover their remaining uncovered branches — all pure-function targets that need minimal mocking.

Purpose: These three modules are the easiest coverage gains (pure functions, no network/filesystem dependencies). Covering them bridges the branch gap needed before threshold raising.
Output: Updated test files with new describe blocks covering previously-uncovered code paths.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-test-coverage-infrastructure/27-01-SUMMARY.md
@src/renderers/utils.ts
@src/renderers/utils.test.ts
@src/config/schema.ts
@src/config/schema.test.ts
@src/context/packer.ts
@src/context/packer.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add buildSummaryLine tests for all switch cases and default</name>
  <files>src/renderers/utils.test.ts</files>
  <action>
Read `src/renderers/utils.ts` to understand the `buildSummaryLine` function signature and its `RenderContext` type. Then add a new `describe('buildSummaryLine()')` block to the existing test file.

**Required test cases (all asserting on the returned string, not mock calls):**

1. **`topic = 'architecture'`** — construct a minimal `RenderContext` with `rounds.r4.data.patterns` containing 3+ pattern objects with `name` fields, and `rounds.r2.data.modules` with 3+ module objects with `name` fields. Assert the returned string contains the project name, pattern names joined by commas, and module names.

2. **`topic = 'overview'`** — construct a `RenderContext` with `staticAnalysis.fileTree.filesByExtension` containing entries like `{'.ts': 50, '.js': 10}`. Assert the returned string mentions the dominant extension (`.ts`), the `fileCount`, and `analyzedAt`.

3. **`topic = 'dependencies'`** — construct a `RenderContext` with `staticAnalysis.dependencies.manifests` containing one manifest with 5 dependencies. Assert the returned string contains `"5 dependencies"` and `"1 manifest"`.

4. **`topic = 'testing'`** — construct a `RenderContext` with `staticAnalysis.tests.summary` having `totalTestFiles: 10, totalTests: 50` and `tests.frameworks: ['vitest']`. Assert the returned string contains file/test counts and framework name.

5. **`default case`** — use `topic = 'unknown-topic'`. Assert the returned string contains the project name, fileCount, and analyzedAt (the fallback message).

For constructing `RenderContext`, create a local `mkRenderContext` factory that provides all required fields with sensible defaults, allowing overrides for each test case. Use inline snapshot assertions for the full returned strings where practical, or explicit `.toContain()` assertions.

Follow existing file conventions: use `describe`/`it` or `describe`/`test`, import from `vitest`, use explicit value assertions.
  </action>
  <verify>
Run `npx vitest run src/renderers/utils.test.ts --coverage` and confirm:
1. All new buildSummaryLine tests pass
2. Coverage for `src/renderers/utils.ts` lines 87-126 (buildSummaryLine) reaches near 100%
  </verify>
  <done>
`buildSummaryLine` is tested for all 5 code paths (4 switch cases + default). Each test asserts on the actual returned string content. `renderers/utils.ts` coverage reaches 90%+ lines.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add EmbeddingConfigSchema superRefine and packer edge-case tests</name>
  <files>src/config/schema.test.ts, src/context/packer.test.ts</files>
  <action>
**Part A: `src/config/schema.test.ts` — EmbeddingConfigSchema superRefine validation**

Read `src/config/schema.ts` to find the `EmbeddingConfigSchema` export (or the `embedding` field within `HandoverConfigSchema`). The superRefine at lines 23-31 validates that `local.model` is required when `mode` is not `remote-only`.

Add a new `describe('embedding config validation')` block with these tests:

1. **Rejects `local-only` mode without `local.model`** — parse config with `embedding: { mode: 'local-only' }` (no `local` or `local: {}`). Assert `success: false` and error path includes `['embedding', 'local', 'model']` or similar.

2. **Rejects `local-preferred` mode without `local.model`** — same as above but `mode: 'local-preferred'`. Assert `success: false`.

3. **Accepts `remote-only` mode without `local.model`** — parse config with `embedding: { mode: 'remote-only' }`. Assert `success: true`.

4. **Accepts `local-only` mode with `local.model` specified** — parse config with `embedding: { mode: 'local-only', local: { model: 'all-MiniLM-L6-v2' } }`. Assert `success: true`.

**Part B: `src/context/packer.test.ts` — edge-case marker detection and greedy section subset**

Read `src/context/packer.ts` carefully around lines 154-177 (marker detection) and lines 366-398 (greedy section subset). Add tests to the existing `packFiles` describe block:

1. **Oversized file with multiple exported functions: greedy section subset includes only sections that fit** — create an oversized file (>8000 tokens) with 3 exported functions at different line ranges. Set budget so signatures fit but only 1-2 section bodies fit. Assert that `tier` is `'full'` and content contains `'// ---'` markers for the included sections but not all 3.

2. **Oversized file with markers in original content: section markers are present in reconstructed output** — construct a file whose content includes `// --- Export:` style markers and verify the packed output preserves or reconstructs them correctly.

3. **Edge: file with AST but no exported symbols produces minimal signature** — create a ParsedFile with private functions/constants only (none exported). Assert `generateSignatureSummary` returns only the header line with no export signatures.

All tests must use explicit value assertions on returned content, not just tier checks.
  </action>
  <verify>
Run `npx vitest run src/config/schema.test.ts src/context/packer.test.ts --coverage` and confirm:
1. All new tests pass
2. `config/schema.ts` branch coverage for the superRefine block improves (lines 23-31 covered)
3. `context/packer.ts` coverage for lines 154-177 and 366-398 improves
  </verify>
  <done>
`config/schema.ts` embedding superRefine validation is tested for all 4 mode combinations. `context/packer.ts` edge-case marker detection and greedy section subset paths are covered with output assertions. Combined branch coverage for these modules exceeds 85%.
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run src/renderers/utils.test.ts src/config/schema.test.ts src/context/packer.test.ts` — all pass
2. `npx vitest run --coverage` — overall coverage remains above 80% thresholds
3. New tests assert on actual return values/output content (grep for `.toContain`, `.toBe`, `.toEqual`, `.toMatchInlineSnapshot` — NOT only `.toHaveBeenCalled`)
</verification>

<success_criteria>
The three pure-function target modules have expanded test coverage covering their previously-uncovered branches. All tests pass. Branch coverage for these modules individually reaches 85%+. The codebase is ready for Plan 27-03 (mock-heavy targets) and Plan 27-04 (threshold raising).
</success_criteria>

<output>
After completion, create `.planning/phases/27-test-coverage-infrastructure/27-02-SUMMARY.md`
</output>
