---
phase: 27-test-coverage-infrastructure
plan: 04
type: execute
wave: 3
depends_on: ["27-02", "27-03"]
files_modified:
  - vitest.config.ts
autonomous: true

must_haves:
  truths:
    - "npm test passes with vitest thresholds at 90% lines, 90% functions, 90% statements, and 85% branches — zero failures"
    - "Thresholds were raised in verifiable incremental steps (80→85→88→90 lines/funcs/stmts, 80→83→85 branches) with npm test passing at each step"
    - "No tests were deleted, weakened, or skipped to meet thresholds"
    - "thresholds.autoUpdate is NOT enabled (per locked decision)"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Final coverage thresholds at 90/90/90/85"
      contains: "lines: 90"
  key_links:
    - from: "vitest.config.ts"
      to: "npm test"
      via: "vitest coverage thresholds enforcement"
      pattern: "thresholds"
---

<objective>
Raise vitest coverage thresholds from the current 80% to the target 90% lines/functions/statements and 85% branches, in verified incremental batches.

Purpose: The research decision mandates raising thresholds in batches (80 -> 85 -> 88 -> 90) gated on confirmed test passage at each step. This ensures no speculative raises and catches any coverage regression immediately.
Output: Updated `vitest.config.ts` with final thresholds at 90/90/90/85.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-test-coverage-infrastructure/27-01-SUMMARY.md
@.planning/phases/27-test-coverage-infrastructure/27-02-SUMMARY.md
@.planning/phases/27-test-coverage-infrastructure/27-03-SUMMARY.md
@vitest.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Raise thresholds incrementally from 80% to 90%/85% with verification at each step</name>
  <files>vitest.config.ts</files>
  <action>
Perform the threshold raise in exactly 3 verified incremental steps. At each step, edit the `thresholds` block in `vitest.config.ts`, run `npm test -- --coverage`, and only proceed to the next step if the command exits 0.

**Step 1: Raise to 85/85/85/80**
```typescript
thresholds: {
  lines: 85,
  functions: 85,
  branches: 80,
  statements: 85,
},
```
Run `npm test -- --coverage`. If it fails:
- Read the coverage report output to identify which metric(s) are below 85%
- Check if any single file is dragging the metric down
- If the file belongs in the exclusion list (0% coverage, integration-only), add it with justification
- If the file is testable, note it as a blocker but do NOT write new tests in this plan — flag it in the summary
- Do NOT proceed to Step 2 until Step 1 passes

**Step 2: Raise to 88/88/88/83**
```typescript
thresholds: {
  lines: 88,
  functions: 88,
  branches: 83,
  statements: 88,
},
```
Run `npm test -- --coverage`. Same gating rules as Step 1.

**Step 3: Raise to final target 90/90/90/85**
```typescript
thresholds: {
  lines: 90,
  functions: 90,
  branches: 85,
  statements: 90,
},
```
Run `npm test -- --coverage`. This is the final target from the roadmap (TEST-01).

**CRITICAL constraints (locked decisions):**
- Do NOT enable `thresholds.autoUpdate` — the upstream vitest#9227 bug strips newlines on config rewrite
- Do NOT raise thresholds speculatively — each step MUST pass before proceeding
- Do NOT delete, skip, or weaken existing tests to meet thresholds
- If the 90/85 target cannot be reached, set thresholds to the highest values that pass and document the gap in the summary

After the final successful raise, update the freeze comment date at the top of the exclusion list to today's date.
  </action>
  <verify>
Run `npm test -- --coverage` with the final thresholds and confirm:
1. Exit code is 0 (all tests pass, all thresholds satisfied)
2. The `thresholds` block reads `lines: 90, functions: 90, branches: 85, statements: 90`
3. `thresholds.autoUpdate` is NOT present in the config
4. The `coverage/coverage-summary.json` output shows all metrics at or above their thresholds
  </verify>
  <done>
vitest.config.ts has thresholds at 90% lines/functions/statements and 85% branches. `npm test -- --coverage` exits 0 with all thresholds passing. `thresholds.autoUpdate` is not enabled. The threshold was raised incrementally with verification at each step. The freeze comment date is updated.
  </done>
</task>

</tasks>

<verification>
1. `npm test -- --coverage` exits 0 with thresholds at 90/90/90/85
2. `cat vitest.config.ts | grep -A4 thresholds` shows `lines: 90, functions: 90, branches: 85, statements: 90`
3. `cat vitest.config.ts | grep autoUpdate` returns nothing (not present)
4. `coverage/coverage-summary.json` exists and shows all metrics above thresholds
</verification>

<success_criteria>
The TEST-01 requirement is satisfied: coverage gate passes at 90%+ lines/functions/statements and 85%+ branches. The threshold was raised in verified batches per the locked decision. No tests were weakened. Phase 27 success criterion #1 is met.
</success_criteria>

<output>
After completion, create `.planning/phases/27-test-coverage-infrastructure/27-04-SUMMARY.md`
</output>
