---
phase: 27-test-coverage-infrastructure
plan: 06
type: execute
wave: 2
depends_on: ["27-05"]
files_modified:
  - src/auth/pkce-login.test.ts
  - src/ai-rounds/validator.test.ts
  - src/orchestrator/dag.test.ts
  - src/renderers/registry.test.ts
  - src/utils/rate-limiter.test.ts
  - src/vector/chunker.test.ts
  - vitest.config.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "`npm test` passes with vitest thresholds at 90/90/90/85 (lines/functions/statements/branches)"
    - "Branch coverage across all modules reaches 85%+ globally"
    - "auth/pkce-login.ts branch coverage exceeds 80% (up from 65.62%)"
    - "ai-rounds/validator.ts branch coverage exceeds 80% (up from 59.37%)"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Updated thresholds at 90/90/90/85"
      contains: "lines: 90"
    - path: "src/auth/pkce-login.test.ts"
      provides: "Additional branch coverage for discovery fallback, token exchange failure, and edge cases"
    - path: "src/ai-rounds/validator.test.ts"
      provides: "Additional branch coverage for extractFilePathClaims, extractImportClaims Round 3 paths"
  key_links:
    - from: "vitest.config.ts"
      to: "npm test"
      via: "vitest thresholds enforcement"
      pattern: "lines: 90"
---

<objective>
Close the remaining coverage gaps across secondary modules and raise vitest thresholds to the phase target of 90/90/90/85. After Plan 27-05 eliminates the MCP coverage deficit, this plan targets the remaining shortfall from auth/pkce-login.ts, ai-rounds/validator.ts, and five smaller modules, then locks the final threshold values.

Purpose: Achieve TEST-01 closure — `npm test` passes at 90/90/90/85.
Output: Expanded tests for 6 modules and updated `vitest.config.ts` with final thresholds.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-test-coverage-infrastructure/27-05-SUMMARY.md
@src/auth/pkce-login.ts
@src/auth/pkce-login.test.ts
@src/ai-rounds/validator.ts
@src/ai-rounds/validator.test.ts
@vitest.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand branch coverage for auth/pkce-login.ts, ai-rounds/validator.ts, and four smaller modules</name>
  <files>src/auth/pkce-login.test.ts, src/ai-rounds/validator.test.ts, src/orchestrator/dag.test.ts, src/renderers/registry.test.ts, src/utils/rate-limiter.test.ts, src/vector/chunker.test.ts</files>
  <action>
**Target: +76 branches needed globally to reach 85%. This task targets secondary modules after Plan 27-05 closes the MCP gap.**

**1. `src/auth/pkce-login.test.ts`** — target: cover lines 291-296, 317, 342 and uncovered branches (currently 65.62% branches)

Add tests for these uncovered paths:
- **Discovery fallback path (lines 263-269):** Mock `discovery` to reject (throw), assert the login still succeeds using hardcoded endpoints. Verify `logger.debug` was called with the discovery failure message. This covers the catch block in the discovery try/catch.
- **Browser open failure path (lines 290-296):** In TTY mode, mock `open` to throw an error. Assert the login still succeeds (falls back to printing URL). Verify `log.info` is called with the auth URL. This covers the catch block in the browser-open try/catch.
- **Missing access_token path (line 317):** Mock `authorizationCodeGrant` to return `{ access_token: '' }` (empty string). Assert it throws with code `AUTH_TOKEN_EXCHANGE_FAILED`. This covers the `!accessToken` branch.
- **Token exchange failure path (lines 220-228 via exchangeAuthorizationCode):** Mock `authorizationCodeGrant` to throw a plain Error. Assert the outer catch wraps it as `AUTH_TOKEN_EXCHANGE_FAILED`.
- **Non-AuthError catch path (line 342):** Mock `authorizationCodeGrant` to throw a non-Error value (e.g., a string). Assert the outer catch wraps it as `AUTH_LOGIN_FAILED`.
- **Callback server 404 path:** The HTTP server returns 404 for non-GET or wrong path. This is harder to cover directly but contributes to branch coverage if the callback server is hit by the test infra.

**2. `src/ai-rounds/validator.test.ts`** — target: cover `extractFilePathClaims` and `extractImportClaims` uncovered branches (currently 59.37% branches)

Add tests for:
- **`extractFilePathClaims` single-segment filter:** Output containing a bare filename like `"config.ts"` (no `/` prefix, doesn't start with `src/`) should NOT be extracted. Verify by checking `validateRoundClaims` returns 0 total for an output containing only non-path-like filenames.
- **`extractImportClaims` Round 3 path (lines 117-133):** Create a Round 3 output with `crossModuleFlows` containing `path` arrays. Test with valid paths (contain `/`), test with paths missing `/` (should be filtered out). Test with non-array `path` field. Test with empty `flows` array.
- **`extractImportClaims` Round 2 non-path relationships:** Relationships where `from` or `to` don't contain `/` should be filtered. Test with `{ from: 'foo', to: 'bar' }` — should not produce import claims.
- **`extractImportClaims` Round 2 non-string fields:** Relationships where `from` or `to` are not strings should be skipped.
- **`extractImportClaims` non-array relationships field:** Output where `relationships` is not an array — should produce 0 claims.

**3. `src/orchestrator/dag.test.ts`** — target: cover ~8 uncovered branches

Review the existing test file. Add tests for edge cases visible in uncovered lines 31-32, 117, 189:
- Empty DAG (no steps registered) — `execute()` returns immediately
- Step with `skipIf` condition returning true — step skipped
- Step dependency that completes with warning — downstream still runs

**4. `src/renderers/registry.test.ts`** — target: cover ~3 uncovered branches and 1 function

Review existing tests. Add tests for:
- Calling `getRenderer` with an unknown renderer name — returns undefined or throws
- Edge case in the registry iteration/selection logic

**5. `src/utils/rate-limiter.test.ts`** — target: cover ~4 uncovered branches

Review existing tests. Add tests for:
- Rate limiter `tryAcquire` when bucket is exactly at capacity
- Rate limiter behavior when `retryAfter` hint is provided
- Edge cases in lines 76 and 108

**6. `src/vector/chunker.test.ts`** — target: cover ~7 uncovered branches

Review existing tests. Add tests for:
- Chunk with no content (empty string input)
- Very small overlap parameter
- Edge case at line 158

Run `npm test -- --coverage` after all additions to verify the cumulative effect. The combined additions from this task plus Plan 27-05 must push global coverage above 90/90/90/85.
  </action>
  <verify>
Run: `npx vitest run src/auth/pkce-login.test.ts src/ai-rounds/validator.test.ts src/orchestrator/dag.test.ts src/renderers/registry.test.ts src/utils/rate-limiter.test.ts src/vector/chunker.test.ts`
All tests pass.
Run: `npm test -- --coverage`
Check that global totals exceed 90% lines, 90% functions, 90% statements, 85% branches.
  </verify>
  <done>
All targeted modules show improved branch coverage. Global coverage exceeds 90/90/90/85 when running `npm test -- --coverage`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Raise vitest thresholds to 90/90/90/85 and verify full suite passes</name>
  <files>vitest.config.ts</files>
  <action>
After Task 1 confirms global coverage exceeds 90/90/90/85:

1. Update `vitest.config.ts` thresholds from `{ lines: 85, functions: 85, branches: 75, statements: 85 }` to `{ lines: 90, functions: 90, branches: 85, statements: 90 }`.
2. Run `npm test` to confirm the full suite passes with the new thresholds enforced.
3. If the full suite passes — done.
4. If the suite fails at 90/90/90/85 but passes at a lower intermediate (e.g., 88/88/88/82), lock to the highest passing values and document the remaining gap. Per locked decision: "Raise thresholds in batches, gated on confirmed test passage — never raise speculatively."

Do NOT enable `thresholds.autoUpdate` — per locked decision about upstream vitest#9227 bug.
  </action>
  <verify>
Run: `npm test`
Exit code 0 with thresholds at 90/90/90/85 (or highest passing values documented).
  </verify>
  <done>
`vitest.config.ts` thresholds are set to 90/90/90/85 and `npm test` passes. TEST-01 is satisfied.
  </done>
</task>

</tasks>

<verification>
1. `npm test` exits 0 with coverage thresholds at `lines: 90, functions: 90, statements: 90, branches: 85`
2. `coverage/coverage-summary.json` total metrics all meet or exceed the threshold values
3. No tests were weakened or removed; no new coverage exclusions were added
</verification>

<success_criteria>
- `vitest.config.ts` thresholds read `{ lines: 90, functions: 90, branches: 85, statements: 90 }`
- `npm test` exits 0 (all tests pass, thresholds enforced)
- auth/pkce-login.ts branch coverage > 80%
- ai-rounds/validator.ts branch coverage > 80%
- Global branch coverage >= 85%
- TEST-01 requirement is satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/27-test-coverage-infrastructure/27-06-SUMMARY.md`
</output>
