---
phase: 27-test-coverage-infrastructure
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp/tools.test.ts
  - src/mcp/errors.test.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "mcp/tools.ts line coverage exceeds 80% (up from 25%)"
    - "mcp/tools.ts branch coverage exceeds 60% (up from 17.64%)"
    - "mcp/errors.ts has 100% line and branch coverage"
    - "All new tests assert on returned structured payloads, not only mock calls"
  artifacts:
    - path: "src/mcp/errors.test.ts"
      provides: "Unit tests for createMcpStructuredError covering HandoverError, plain Error, and non-Error branches"
    - path: "src/mcp/tools.test.ts"
      provides: "Expanded tests covering semantic_search, QA stream lifecycle, and cancel tool handlers"
  key_links:
    - from: "src/mcp/tools.test.ts"
      to: "src/mcp/tools.ts"
      via: "imports of helper functions and registerMcpTools with mocked McpServer"
      pattern: "registerMcpTools|createRegenerationToolHandlers"
    - from: "src/mcp/errors.test.ts"
      to: "src/mcp/errors.ts"
      via: "import createMcpStructuredError"
      pattern: "createMcpStructuredError"
---

<objective>
Close the largest coverage gap in the codebase: `mcp/tools.ts` (25% lines, 17.64% branches) and `mcp/errors.ts` (40% lines, 33.33% branches). These two files account for ~135 uncovered lines and ~88 uncovered branches — the single biggest blocker to reaching 90/90/90/85 thresholds.

Purpose: Eliminate the MCP module coverage deficit that prevents TEST-01 closure.
Output: Expanded `src/mcp/tools.test.ts` and new `src/mcp/errors.test.ts` with output-assertion tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-test-coverage-infrastructure/27-03-SUMMARY.md
@src/mcp/tools.ts
@src/mcp/errors.ts
@src/mcp/tools.test.ts
@src/utils/errors.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create mcp/errors.test.ts and expand mcp/tools.test.ts with helper and registerMcpTools coverage</name>
  <files>src/mcp/errors.test.ts, src/mcp/tools.test.ts</files>
  <action>
**1. Create `src/mcp/errors.test.ts`** with tests for `createMcpStructuredError`:
- HandoverError input: assert returned `{ code, message, action }` uses `error.code`, `error.message`, `error.fix`
- HandoverError with undefined code: assert fallback to `'MCP_SERVE_ERROR'`
- Plain Error input: assert `code === 'MCP_SERVE_ERROR'`, `message === error.message`, `action` contains retry text
- Non-Error input (string): assert `code === 'MCP_SERVE_ERROR'`, `message === String(input)`, `action` contains retry text
- Non-Error input (number/null/undefined): same pattern, covers the final `else` branch

This covers all 3 branches in errors.ts (HandoverError, Error, non-Error) plus the `error.code ?? 'MCP_SERVE_ERROR'` null-coalescing branch.

**2. Expand `src/mcp/tools.test.ts`** to cover the `registerMcpTools` function and its internal tool handlers. Strategy: mock the `McpServer` object by capturing the tool handler callbacks registered via `server.registerTool`, then invoke them directly with test inputs.

Create a mock McpServer factory:
```typescript
function createMockServer() {
  const tools = new Map<string, Function>();
  return {
    registerTool: vi.fn((name: string, _schema: unknown, handler: Function) => {
      tools.set(name, handler);
    }),
    getHandler: (name: string) => tools.get(name),
  };
}
```

Add tests for **semantic_search** tool handler:
- Valid input with successful search: mock `searchFn` to return results, assert `structuredContent.ok === true` with results array
- Valid input with empty results: assert `structuredContent.results` is empty array
- Invalid input (empty query): assert `isError === true` and `structuredContent.error.code === 'SEARCH_INVALID_INPUT'`
- Search function throws: assert error is wrapped via `createMcpStructuredError`
- Default limit applied when omitted: verify `searchFn` called with `topK: 10`

Add tests for **qa_stream_start** tool handler:
- Invalid input (empty query): assert validation error payload with code `'QA_STREAM_INVALID_INPUT'`
- Note: The full success path requires mocking `createQaStreamingSessionManager`, which is called at `registerMcpTools` time. Mock the `../qa/streaming-session.js` module to return a fake session manager that tracks calls. Assert the success payload shape includes `ok, sessionId, state, lastSequence, events`.
- QaSessionStoreError (SESSION_NOT_FOUND): assert mapped error payload
- Generic error: assert fallback `createMcpStructuredError` wrapping

Add tests for **qa_stream_status** tool handler:
- Valid input: mock session manager `getSessionState` and `resumeSession`, assert success payload shape
- Invalid input (empty sessionId): assert validation error
- QaSessionStoreError: assert mapped error

Add tests for **qa_stream_resume** tool handler:
- Invalid input: assert validation error
- Invalid cursor (lastAckSequence > lastSequence): assert `QA_STREAM_INVALID_CURSOR` error
- Note: The running-session-with-polling path is complex; covering the validation and cursor-check branches is sufficient for this plan.

Add tests for **qa_stream_cancel** tool handler:
- Valid cancel: mock session manager `cancelSession`, assert success payload shape with `ok, sessionId, state`
- Invalid input (empty sessionId): assert validation error
- QaSessionStoreError (SESSION_NOT_FOUND): assert mapped error

Add tests for **helper functions** (already partially covered but ensure branch coverage):
- `getProgressToken`: already internal but exercised through qa_stream_start/resume extra parameter
- `mapQaSessionStoreError`: cover both `SESSION_NOT_FOUND` and `SESSION_SEQUENCE_MISMATCH` codes, plus default branch
- `hasTerminalEvent`: cover arrays with `final`, `cancelled`, `error` events, and array with no terminal event

Use `vi.mock('../qa/streaming-session.js')` and `vi.mock('../vector/query-engine.js')` to isolate from real dependencies. The mock session manager should be controlled per-test via `mockReturnValue`/`mockImplementation`.

All assertions MUST check `structuredContent` payload shape (not just `isError` flag) per phase context decisions on output-assertion testing.
  </action>
  <verify>
Run: `npx vitest run src/mcp/errors.test.ts src/mcp/tools.test.ts`
All tests pass. No import errors or type failures.
  </verify>
  <done>
`mcp/errors.ts` has 100% line/branch coverage. `mcp/tools.ts` line coverage exceeds 80% and branch coverage exceeds 60%. All assertions verify returned payload shapes.
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run src/mcp/errors.test.ts src/mcp/tools.test.ts` — all tests pass
2. `npm test -- --coverage` — mcp/errors.ts shows 100%/100%/100%/100%, mcp/tools.ts shows >80% lines and >60% branches
3. Global coverage metrics improve significantly toward 90/90/90/85 target
</verification>

<success_criteria>
- `src/mcp/errors.test.ts` exists and covers all three branches of `createMcpStructuredError`
- `src/mcp/tools.test.ts` covers semantic_search, qa_stream_start, qa_stream_status, qa_stream_resume, qa_stream_cancel handlers with output assertions
- mcp/errors.ts coverage: 100% lines, 100% branches
- mcp/tools.ts coverage: >80% lines, >60% branches
- Full suite still passes with current 85/85/85/75 thresholds
</success_criteria>

<output>
After completion, create `.planning/phases/27-test-coverage-infrastructure/27-05-SUMMARY.md`
</output>
