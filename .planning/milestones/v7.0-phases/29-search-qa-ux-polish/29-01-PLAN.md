---
phase: 29-search-qa-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/vector/vector-store.ts
  - src/vector/query-engine.ts
  - src/cli/index.ts
  - src/cli/search.ts
autonomous: true

must_haves:
  truths:
    - "`handover search --help` lists the valid --type values in the options description"
    - "A search returning zero results displays available doc types sourced live from the vector store and total indexed document count"
    - "When the index is completely empty (0 documents), guidance suggests running `handover generate` first"
    - "A search where the best-match cosine distance exceeds the quality threshold displays an inline text warning with numeric distance and actionable suggestion"
  artifacts:
    - path: "src/vector/vector-store.ts"
      provides: "getDistinctDocTypes() method returning live doc types from database"
      contains: "getDistinctDocTypes"
    - path: "src/vector/query-engine.ts"
      provides: "Extended search result shape with availableDocTypes and totalIndexed fields"
      contains: "availableDocTypes"
    - path: "src/cli/index.ts"
      provides: "--type option description listing valid values"
      contains: "KNOWN_DOC_TYPES"
    - path: "src/cli/search.ts"
      provides: "Zero-results guidance with live doc types, empty-index guidance, and distance warning"
      contains: "distance"
  key_links:
    - from: "src/vector/vector-store.ts"
      to: "vec_chunks table"
      via: "SELECT DISTINCT doc_type query"
      pattern: "getDistinctDocTypes"
    - from: "src/vector/query-engine.ts"
      to: "src/vector/vector-store.ts"
      via: "calls getDistinctDocTypes() and getChunkCount()"
      pattern: "vectorStore\\.getDistinctDocTypes"
    - from: "src/cli/index.ts"
      to: "src/vector/query-engine.ts"
      via: "imports exported KNOWN_DOC_TYPES for --type help text"
      pattern: "import.*KNOWN_DOC_TYPES.*from.*query-engine"
    - from: "src/cli/search.ts"
      to: "src/vector/query-engine.ts"
      via: "imports exported DISTANCE_WARNING_THRESHOLD and reads availableDocTypes/totalIndexed from result"
      pattern: "DISTANCE_WARNING_THRESHOLD|result\\.availableDocTypes|result\\.totalIndexed"
    - from: "src/vector/query-engine.ts"
      to: "searchDocuments return path"
      via: "empty index returns result with totalIndexed:0 instead of throwing"
      pattern: "totalIndexed: 0"
---

<objective>
Add `--type` help text listing valid values, zero-results guidance with live doc types from the vector store, empty-index guidance suggesting `handover generate`, and distance warning for low-relevance results to the CLI search output.

Purpose: Users currently see a bare "No results found" message on zero results and have no quality signal when results are poor. This plan surfaces actionable guidance and quality warnings per SRCH-01, SRCH-02, SRCH-03.

Output: Updated CLI search output with three new UX signals: help text, zero-results guidance, and distance warning.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/vector/vector-store.ts
@src/vector/query-engine.ts
@src/cli/index.ts
@src/cli/search.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add VectorStore.getDistinctDocTypes() and extend search result shape</name>
  <files>src/vector/vector-store.ts, src/vector/query-engine.ts</files>
  <action>
  **VectorStore (src/vector/vector-store.ts):**
  Add a `getDistinctDocTypes(): string[]` method to the `VectorStore` class.
  - Query: `SELECT DISTINCT doc_type FROM vec_chunks ORDER BY doc_type ASC`
  - Return the array of doc_type strings
  - Follow the existing guard pattern: throw `Error('Database not open. Call open() first.')` if `this.db` is null

  **Query engine (src/vector/query-engine.ts):**
  1. Add `export` to the existing `KNOWN_DOC_TYPES` constant — change `const KNOWN_DOC_TYPES = [` to `export const KNOWN_DOC_TYPES = [`. This is additive and does not affect existing usages within the module. Do NOT change the array contents or its usage for type validation — the live doc types are ADDITIONAL runtime data for display, not a replacement for validation.
  2. Define an exported named constant: `export const DISTANCE_WARNING_THRESHOLD = 0.65` near the top of the file (after `KNOWN_DOC_TYPES`).
  3. Extend `SearchDocumentsResult` interface with two new fields (NOT optional — always present):
     - `availableDocTypes: string[]` — live doc types from vector store
     - `totalIndexed: number` — total chunk count
  4. In `searchDocuments()`, **remove** the `if (indexedChunks === 0) { throw new HandoverError(...'SEARCH_INDEX_EMPTY'...) }` block entirely. Replace it with an early-return that short-circuits the embedding call:
     ```typescript
     if (indexedChunks === 0) {
       return {
         query,
         topK,
         totalMatches: 0,
         matches: [],
         filters: { types: normalizedTypes },
         availableDocTypes: [],
         totalIndexed: 0,
       };
     }
     ```
     This makes `result.totalIndexed === 0` reachable downstream instead of throwing.
  5. After the early-return guard, call `vectorStore.getDistinctDocTypes()` to get live doc types and include `availableDocTypes` and `totalIndexed: indexedChunks` in the normal return object (always, alongside existing fields).
  </action>
  <verify>
  Run `npx tsc --noEmit` to confirm no type errors. Run `npm test` to confirm existing tests pass.
  </verify>
  <done>
  `VectorStore` has a `getDistinctDocTypes()` method. `SearchDocumentsResult` includes `availableDocTypes` and `totalIndexed` fields. All existing tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add --type help text, zero-results guidance, and distance warning to CLI</name>
  <files>src/cli/index.ts, src/cli/search.ts</files>
  <action>
  **CLI index (src/cli/index.ts):**
  1. Import `KNOWN_DOC_TYPES` from `../vector/query-engine.js` at the top of the file.
  2. Update the `--type` option description from `'Filter by document type (repeatable)'` to include the valid values. Format: `'Filter by document type (repeatable). Valid types: project-overview, getting-started, architecture, file-structure, features, modules, dependencies, environment, edge-cases-and-gotchas, tech-debt-and-todos, conventions, testing-strategy, deployment'`. Generate this string dynamically from the imported `KNOWN_DOC_TYPES` array: `` `Filter by document type (repeatable). Valid types: ${KNOWN_DOC_TYPES.join(', ')}` ``

  **CLI search (src/cli/search.ts):**
  1. Import `DISTANCE_WARNING_THRESHOLD` from `../vector/query-engine.js`.
  2. **Zero-results guidance in `runFastMode()`:** Replace the current zero-results block (lines 139-147) with enhanced guidance:
     - If `result.totalIndexed === 0` (or undefined for safety): print `No results found. The search index is empty.` then `Run 'handover generate' to analyze your codebase, then 'handover reindex' to build the search index.`
     - If `result.totalIndexed > 0` but `result.totalMatches === 0`: print `No results found ({N} documents indexed).` where N is `result.totalIndexed`. Then print `Available types: {types}` using `result.availableDocTypes` (joined with `, `). Then print the existing refinement tips: use more specific keywords, increase --top-k, remove/broaden --type filters.
  3. **Distance warning in `runFastMode()`:** After the `for` loop printing results (before the "Showing X of Y" summary line), check if the first match's distance exceeds `DISTANCE_WARNING_THRESHOLD`. If so, insert an inline warning line:
     - Use the warning symbol from `SYMBOLS` (import from `../ui/formatters.js`): ``console.log(`${SYMBOLS.warning} Low relevance (distance: ${result.matches[0].distance.toFixed(2)}). Try a more specific query or different --type`);``
     - Place this line BEFORE the empty line and summary count line.
  </action>
  <verify>
  Run `npx tsc --noEmit` — no type errors. Run `npm test` — all pass. Run `npx tsx src/cli/index.ts search --help` and confirm `--type` description includes valid type names.
  </verify>
  <done>
  `handover search --help` shows valid --type values. Zero-results output distinguishes empty index from no matches and shows live doc types. Low-relevance results display a distance warning with actionable suggestion.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm test` passes with no regressions
3. `npx tsx src/cli/index.ts search --help` output includes valid type names in `--type` description
4. Code review: `DISTANCE_WARNING_THRESHOLD` is a named constant (not magic number), `getDistinctDocTypes()` uses a SQL query (not hardcoded list), zero-results path distinguishes empty index from no-matches
</verification>

<success_criteria>
- SRCH-01: `--type` valid values shown in help output
- SRCH-02: Zero-results search displays available doc types as guidance with indexed document count
- SRCH-03: Low-relevance results display distance warning when best match exceeds threshold
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/29-search-qa-ux-polish/29-01-SUMMARY.md`
</output>
